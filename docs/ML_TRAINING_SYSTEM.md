# ML 학습 시스템 가이드

> **작성일**: 2025-12-24
> **상태**: 운영 중 (Production)

---

## 목차

1. [시스템 개요](#시스템-개요)
2. [ML 모델 구성](#ml-모델-구성)
3. [학습 파이프라인](#학습-파이프라인)
4. [자동 재학습 시스템](#자동-재학습-시스템)
5. [배포 프로세스](#배포-프로세스)
6. [모니터링 및 검증](#모니터링-및-검증)
7. [문제 해결](#문제-해결)

---

## 시스템 개요

### 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                    ML Training System                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐    ┌──────────────────┐                   │
│  │  Bitget Public   │───▶│  Feature Pipeline │                   │
│  │  API (무인증)    │    │  (86개 피처 추출) │                   │
│  └──────────────────┘    └────────┬─────────┘                   │
│                                   │                              │
│                                   ▼                              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                 5개 LightGBM 모델                         │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  1. Direction    - 방향 예측 (long/short/neutral)        │   │
│  │  2. Volatility   - 변동성 레벨 (low/normal/high/extreme) │   │
│  │  3. Timing       - 진입 타이밍 검증 (good/ok/bad)        │   │
│  │  4. StopLoss     - 최적 손절 % (0.5~5%)                  │   │
│  │  5. PositionSize - 최적 포지션 크기 % (5~40%)            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                   │                              │
│                                   ▼                              │
│  ┌──────────────────┐    ┌──────────────────┐                   │
│  │  EnsemblePredictor│───▶│  ETH AI Strategy │                   │
│  │  (통합 예측)      │    │  (거래 결정)      │                   │
│  └──────────────────┘    └──────────────────┘                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 핵심 컴포넌트

| 컴포넌트 | 파일 위치 | 역할 |
|---------|----------|------|
| Feature Pipeline | `src/ml/features/feature_pipeline.py` | 86개 기술적 피처 추출 |
| Ensemble Predictor | `src/ml/models/ensemble_predictor.py` | 5개 모델 통합 예측 |
| Training Pipeline | `scripts/train_ml_pipeline.py` | 수동 학습 스크립트 |
| Auto Retrain | `scripts/auto_retrain.py` | 자동 재학습 스크립트 |

---

## ML 모델 구성

### 5개 LightGBM 모델

#### 1. Direction Model (Classifier)
- **목적**: 가격 방향 예측
- **출력**: `long` / `short` / `neutral`
- **신뢰도**: 확률값 (0.0 ~ 1.0)

#### 2. Volatility Model (Classifier)
- **목적**: 변동성 수준 예측
- **출력**: `low` / `normal` / `high` / `extreme`
- **용도**: 레버리지 조절, 포지션 크기 결정

#### 3. Timing Model (Classifier)
- **목적**: 진입 타이밍 검증
- **출력**: `good` / `ok` / `bad`
- **용도**: 진입 신호 필터링

#### 4. StopLoss Model (Regressor)
- **목적**: 최적 손절 비율 예측
- **출력**: 0.5% ~ 5.0%
- **용도**: 동적 손절 설정

#### 5. PositionSize Model (Regressor)
- **목적**: 최적 포지션 크기 예측
- **출력**: 5% ~ 40%
- **용도**: 리스크 관리

### 피처 구성 (86개)

```python
# 기술적 지표 (50개)
- RSI, MACD, Bollinger Bands, ATR
- EMA (5, 10, 20, 50, 100, 200)
- Volume 관련 지표
- 모멘텀 지표

# 구조적 피처 (20개)
- 캔들 패턴 (Doji, Hammer, Engulfing 등)
- 지지/저항 레벨
- 추세 강도

# 멀티타임프레임 피처 (16개)
- 상위 타임프레임 추세
- 다중 RSI 비교
```

### 모델 파일 저장 위치

```
backend/src/ml/saved_models/
├── lightgbm_direction.txt        # 방향 모델 (335KB)
├── lightgbm_volatility.txt       # 변동성 모델 (1.2MB)
├── lightgbm_timing.txt           # 타이밍 모델 (25KB)
├── lightgbm_stoploss.txt         # 손절 모델 (32KB)
├── lightgbm_position_size.txt    # 포지션 크기 모델 (499KB)
├── direction_feature_importance.csv
├── volatility_feature_importance.csv
├── timing_feature_importance.csv
├── stop_loss_feature_importance.csv
└── position_size_feature_importance.csv
```

---

## 학습 파이프라인

### 수동 학습 실행

```bash
# 기본 학습 (60일 ETHUSDT 데이터)
cd backend
python scripts/train_ml_pipeline.py

# 심볼과 기간 지정
python scripts/train_ml_pipeline.py --symbol BTCUSDT --days 90

# 빠른 테스트 (7일 데이터, 100 라운드)
python scripts/train_ml_pipeline.py --days 7 --rounds 100 --quick
```

### 학습 프로세스

```
1. 데이터 수집
   └── Bitget 공개 API로 캔들 데이터 수집 (인증 불필요)

2. 피처 추출
   └── FeaturePipeline으로 86개 피처 생성

3. 라벨 생성
   ├── Direction: 미래 수익률 기반 (up/neutral/down)
   ├── Volatility: ATR 기반 변동성 분류
   ├── Timing: 진입 후 성과 기반
   ├── StopLoss: 최적 손절 지점 계산
   └── PositionSize: 리스크 조정 수익률 기반

4. 모델 학습
   └── LightGBM 앙상블 학습 (5개 모델)

5. 모델 저장
   └── LightGBM 네이티브 형식 (.txt)
```

### 학습 파라미터

```python
# LightGBM 기본 설정
params = {
    "objective": "multiclass",  # 분류 모델
    "num_class": 3,             # 클래스 수
    "boosting_type": "gbdt",
    "num_leaves": 31,
    "learning_rate": 0.05,
    "feature_fraction": 0.9,
    "bagging_fraction": 0.8,
    "bagging_freq": 5,
    "verbose": -1,
    "num_threads": 4,
}
```

---

## 자동 재학습 시스템

### Cron 설정

```bash
# 서버 crontab 설정 (이미 적용됨)
0 6 * * * docker exec trading-backend python3 /app/scripts/auto_retrain.py >> /var/log/ml_retrain.log 2>&1
```

| 항목 | 값 |
|------|-----|
| 실행 시간 | 매일 06:00 UTC (15:00 KST) |
| 학습 데이터 | 최근 60일 |
| 심볼 | ETHUSDT |
| 로그 | `/var/log/ml_retrain.log` |

### 성능 기준치

```python
PERFORMANCE_THRESHOLDS = {
    "direction_accuracy": 0.55,      # 방향 예측 정확도 최소 55%
    "timing_accuracy": 0.45,         # 타이밍 예측 정확도 최소 45%
    "volatility_accuracy": 0.70,     # 변동성 예측 정확도 최소 70%
    "stop_loss_rmse": 0.50,          # SL RMSE 최대 0.5
    "position_size_rmse": 5.0,       # 포지션 사이즈 RMSE 최대 5
}
```

### 자동 재학습 플로우

```
┌─────────────────────────────────────────────────────────────┐
│                  Daily Auto-Retrain (15:00 KST)              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. 데이터 수집 (60일)                                       │
│         │                                                    │
│         ▼                                                    │
│  2. 현재 모델 성능 검증                                      │
│         │                                                    │
│         ├── 성능 양호 ──▶ 스킵 (재학습 불필요)              │
│         │                                                    │
│         └── 성능 저하 ──▶ 재학습 진행                       │
│                   │                                          │
│                   ▼                                          │
│  3. 새 모델 학습                                             │
│         │                                                    │
│         ▼                                                    │
│  4. 신규 모델 검증                                           │
│         │                                                    │
│         ├── 기존보다 우수 ──▶ 모델 교체                     │
│         │                                                    │
│         └── 기존보다 열등 ──▶ 기존 유지 + 알림              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 배포 프로세스

### 방법 1: 서버 자동 학습 (권장)

서버에서 자동으로 학습하므로 별도 배포 불필요.

```bash
# Cron이 매일 자동 실행
# 모델은 컨테이너 내부에 저장되어 즉시 적용
```

### 방법 2: 로컬에서 학습 후 배포

```bash
# 1. 로컬에서 학습
cd backend
python scripts/train_ml_pipeline.py --symbol ETHUSDT --days 60

# 2. 서버로 모델 전송
rsync -avz src/ml/saved_models/ \
  root@158.247.245.197:/root/auto-dashboard/backend/src/ml/saved_models/ \
  -e "sshpass -p 'PASSWORD' ssh -o StrictHostKeyChecking=no"

# 3. 백엔드 재시작 (모델 로드)
ssh root@158.247.245.197 "docker restart trading-backend"
```

### 방법 3: 전체 재빌드 배포

```bash
# 1. 서버 모델 백업 (중요!)
rsync -avz -e "sshpass -p 'PASSWORD' ssh -o StrictHostKeyChecking=no" \
  root@158.247.245.197:/root/auto-dashboard/backend/src/ml/saved_models/ \
  ./backend/src/ml/saved_models/

# 2. 코드 동기화
rsync -avz --exclude 'node_modules' --exclude '.git' \
  --exclude '__pycache__' --exclude '*.pyc' \
  -e "sshpass -p 'PASSWORD' ssh -o StrictHostKeyChecking=no" \
  ./ root@158.247.245.197:/root/auto-dashboard/

# 3. 재빌드 및 재시작
ssh root@158.247.245.197 "cd /root/auto-dashboard && \
  docker compose build --no-cache backend && \
  docker compose up -d backend"
```

### 주의사항

> **컨테이너 재빌드 시 모델 백업 필수!**
>
> `docker compose build`는 새 이미지를 생성하므로, 서버에서 학습된 최신 모델이 로컬 코드의 모델로 덮어씌워질 수 있습니다.

---

## 모니터링 및 검증

### 모델 로드 확인

```bash
# 로그에서 확인
docker logs trading-backend 2>&1 | grep -E "Loaded.*models"

# 정상 출력 예시
# Loaded 5/5 models
# EnsemblePredictor initialized: models_loaded=True
```

### ML 예측 확인

```bash
# 실시간 예측 로그
docker logs trading-backend 2>&1 | grep "ML Prediction" | tail -5

# 출력 예시
# 📊 ML Prediction: ETHUSDT -> Dir:short(36%), Vol:normal, Timing:True, Combined:59%
```

### 자동 재학습 로그 확인

```bash
# 서버에서 확인
cat /var/log/ml_retrain.log | tail -50
```

### 수동 모델 테스트

```bash
# 컨테이너 내부에서 테스트
docker exec trading-backend python3 -c "
from src.ml.models.ensemble_predictor import EnsemblePredictor
predictor = EnsemblePredictor()
print('Models loaded:', predictor.models_loaded)
print('Training features:', len(predictor.training_features) if predictor.training_features else 0)
"
```

---

## 문제 해결

### 1. 모델 로드 실패 (0/5 models)

**원인**: 모델 파일 없음 또는 경로 불일치

```bash
# 모델 파일 확인
docker exec trading-backend ls -la /app/src/ml/saved_models/

# 해결: 모델 학습 또는 복사
docker exec trading-backend python3 /app/scripts/train_ml_pipeline.py --days 30
```

### 2. LightGBM 미설치

**원인**: 컨테이너에 LightGBM 미설치

```bash
# 확인
docker exec trading-backend python3 -c "import lightgbm; print(lightgbm.__version__)"

# 해결: requirements.txt에 lightgbm 추가 후 재빌드
```

### 3. 피처 수 불일치

**원인**: 학습 시 피처와 예측 시 피처가 다름

```bash
# 학습 피처 확인
docker exec trading-backend cat /app/src/ml/saved_models/direction_feature_importance.csv | wc -l

# 예측 피처 확인 (로그)
docker logs trading-backend 2>&1 | grep "Extracted.*features"
```

### 4. 자동 재학습 미실행

**원인**: Cron 미설정 또는 스크립트 오류

```bash
# Cron 확인
crontab -l | grep retrain

# 수동 실행 테스트
docker exec trading-backend python3 /app/scripts/auto_retrain.py --validate-only
```

---

## 참고 자료

- [LightGBM 공식 문서](https://lightgbm.readthedocs.io/)
- [Bitget API 문서](https://www.bitget.com/api-doc/)
- [프로젝트 CLAUDE.md](/CLAUDE.md) - 전체 시스템 가이드

---

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2025-12-24 | 초기 문서 작성 |
| 2025-12-24 | 자동 재학습 Cron 설정 완료 |
| 2025-12-24 | 5개 LightGBM 모델 학습 및 배포 완료 |

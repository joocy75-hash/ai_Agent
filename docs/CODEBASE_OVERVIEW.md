# 📚 코드베이스 전체 개요 (Codebase Overview)

> **⚠️ 새로운 작업자를 위한 필독 문서**  
> 이 문서를 먼저 읽고 작업을 시작하세요!

---

## 📋 목차

1. [프로젝트 소개](#프로젝트-소개)
2. [기술 스택](#기술-스택)
3. [디렉토리 구조](#디렉토리-구조)
4. [핵심 파일 맵](#핵심-파일-맵)
5. [데이터 흐름](#데이터-흐름)
6. [다중 사용자 시스템](#다중-사용자-시스템)
7. [AI 에이전트 시스템](#ai-에이전트-시스템)
8. [작업별 참고 문서](#작업별-참고-문서)
9. [자주 사용하는 명령어](#자주-사용하는-명령어)
10. [주의사항](#주의사항)

---

## 🎯 프로젝트 소개

### 한 줄 요약

**Bitget 거래소 기반 암호화폐 자동매매 플랫폼** (AI 봇 + 그리드 봇)

### 주요 기능

| 기능 | 설명 | 상태 |
|------|------|------|
| 🤖 AI 자동매매 봇 | DeepSeek AI 기반 시장 분석 및 자동 거래 | ✅ 완료 |
| 📊 그리드 봇 | 범위 거래 자동화 (등차/등비 그리드) | ✅ 완료 |
| 👥 다중 봇 시스템 | 사용자당 최대 10개 봇 동시 운영 | ✅ 완료 |
| 📈 백테스팅 | 과거 데이터 기반 전략 검증 | ✅ 완료 |
| 🔔 텔레그램 알림 | 거래 알림 및 봇 상태 알림 | ✅ 완료 |
| 🎨 웹 대시보드 | 사용자/관리자 웹 인터페이스 | ✅ 완료 |

---

## 🛠️ 기술 스택

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  React 18       │  │  Vite           │  │  TradingView    │  │
│  │  (사용자 UI)     │  │  (빌드 도구)     │  │  (차트)          │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
│  Port: 3000 (User) / 4000 (Admin)                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼ HTTP/WebSocket
┌─────────────────────────────────────────────────────────────────┐
│                         BACKEND                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  FastAPI        │  │  SQLAlchemy     │  │  DeepSeek AI    │  │
│  │  (REST API)     │  │  (ORM)          │  │  (LLM)          │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
│  Port: 8000                                                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATA LAYER                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  PostgreSQL     │  │  Redis          │  │  Bitget API     │  │
│  │  (메인 DB)       │  │  (캐시/세션)     │  │  (거래소)        │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
│  Port: 5432              Port: 6379                             │
└─────────────────────────────────────────────────────────────────┘
```

### 버전 정보

| 기술 | 버전 |
|------|------|
| Python | 3.11 |
| Node.js | 20.x |
| PostgreSQL | 15 |
| Redis | 7 |
| Docker | 최신 |

---

## 📁 디렉토리 구조

```
auto-dashboard/
│
├── 📂 backend/                    # ⭐ 백엔드 (FastAPI)
│   ├── src/
│   │   ├── api/                   # API 엔드포인트
│   │   ├── agents/                # AI 에이전트 시스템
│   │   ├── database/              # DB 모델 & 세션
│   │   ├── services/              # 비즈니스 로직
│   │   ├── strategies/            # 트레이딩 전략
│   │   ├── schemas/               # Pydantic 스키마
│   │   ├── middleware/            # 미들웨어
│   │   ├── utils/                 # 유틸리티
│   │   ├── websockets/            # WebSocket 서버
│   │   ├── workers/               # 백그라운드 워커
│   │   ├── config.py              # 설정
│   │   └── main.py                # ⭐ 앱 진입점
│   │
│   ├── alembic/                   # DB 마이그레이션
│   ├── tests/                     # 테스트
│   └── requirements.txt           # Python 의존성
│
├── 📂 frontend/                   # ⭐ 사용자 프론트엔드 (React)
│   ├── src/
│   │   ├── api/                   # API 클라이언트
│   │   ├── components/            # React 컴포넌트
│   │   ├── pages/                 # 페이지 컴포넌트
│   │   ├── context/               # React Context
│   │   ├── hooks/                 # Custom Hooks
│   │   ├── App.jsx                # ⭐ 앱 루트
│   │   └── main.jsx               # 진입점
│   │
│   └── package.json               # Node.js 의존성
│
├── 📂 admin-frontend/             # 관리자 프론트엔드
│   └── (frontend와 유사한 구조)
│
├── 📂 docs/                       # ⭐ 문서
│   ├── BACKEND_ARCHITECTURE.md    # 백엔드 아키텍처
│   ├── FRONTEND_ARCHITECTURE.md   # 프론트엔드 아키텍처
│   ├── MULTI_BOT_01_OVERVIEW.md   # 다중 봇 시스템 개요
│   ├── MULTI_BOT_02_DATABASE.md   # 다중 봇 DB 설계
│   ├── MULTI_BOT_03_IMPLEMENTATION.md # 다중 봇 구현
│   └── CODEBASE_OVERVIEW.md       # 이 문서
│
├── 📄 CLAUDE.md                   # ⭐⭐ AI 개발 필독 가이드
├── 📄 docker-compose.yml          # Docker 구성
└── 📄 deploy-production.sh        # 배포 스크립트
```

---

## 🗺️ 핵심 파일 맵

### 🔴 백엔드 핵심 파일

#### API 엔드포인트 (`backend/src/api/`)

| 파일 | 역할 | 주요 엔드포인트 |
|------|------|----------------|
| `auth.py` | 인증 | `/auth/login`, `/auth/register` |
| `bot.py` | 봇 제어 (레거시) | `/bot/start`, `/bot/stop` |
| `bot_instances.py` | ⭐ 다중 봇 관리 | `/bot-instances/`, `/bot-instances/{id}/start` |
| `grid_bot.py` | 그리드 봇 | `/grid-bot/config`, `/grid-bot/orders` |
| `strategy.py` | 전략 관리 | `/strategy/list`, `/ai/strategies/generate` |
| `order.py` | 주문/거래 | `/order/history`, `/trades/positions` |
| `account.py` | 계정 관리 | `/account/balance`, `/account/save_keys` |
| `backtest.py` | 백테스팅 | `/backtest/start`, `/backtest/history` |
| `admin_*.py` | 관리자 전용 | 관리자 기능들 |

#### 서비스 레이어 (`backend/src/services/`)

| 파일 | 역할 | 중요도 |
|------|------|--------|
| `bot_runner.py` | ⭐⭐⭐ 봇 실행 엔진 | 핵심 |
| `grid_bot_runner.py` | 그리드 봇 실행 | 핵심 |
| `strategy_loader.py` | ⭐⭐ 전략 로딩 | 핵심 |
| `allocation_manager.py` | 잔고 할당 관리 | 중요 |
| `bot_isolation_manager.py` | 봇 격리 관리 | 중요 |
| `bot_recovery_manager.py` | 봇 복구/에러 처리 | 중요 |
| `deepseek_service.py` | ⭐ DeepSeek AI 호출 | 핵심 |
| `bitget_rest.py` | Bitget REST API | 핵심 |
| `bitget_ws.py` | Bitget WebSocket | 핵심 |
| `candle_cache.py` | 캔들 데이터 캐시 | 중요 |
| `chart_data_service.py` | 차트 데이터 | 중요 |

#### 전략 (`backend/src/strategies/`)

| 파일 | 전략 유형 | 설명 |
|------|----------|------|
| `eth_ai_autonomous_40pct_strategy.py` | ⭐ ETH 자율 40% | 메인 프로덕션 전략 |
| `adaptive_market_regime_fighter.py` | 적응형 전략 | 시장 환경 기반 |
| `autonomous_30pct_strategy.py` | 30% 자율 전략 | 보수적 버전 |
| `proven_conservative_strategy.py` | 보수적 전략 | EMA 크로스오버 |

#### 에이전트 시스템 (`backend/src/agents/`)

| 디렉토리 | 에이전트 | 역할 |
|----------|---------|------|
| `market_regime/` | MarketRegimeAgent | 시장 환경 분석 (AI) |
| `signal_validator/` | SignalValidatorAgent | 시그널 검증 |
| `risk_monitor/` | RiskMonitorAgent | 리스크 감시 |
| `portfolio_optimizer/` | PortfolioOptimizerAgent | 포트폴리오 최적화 |
| `base.py` | BaseAgent | 에이전트 베이스 클래스 |

#### 데이터베이스 (`backend/src/database/`)

| 파일 | 역할 |
|------|------|
| `models.py` | ⭐ SQLAlchemy 모델 정의 |
| `db.py` | DB 연결 설정 |
| `session.py` | 세션 관리 |

### 🟢 프론트엔드 핵심 파일

#### 페이지 (`frontend/src/pages/`)

| 파일 | 페이지 | 설명 |
|------|--------|------|
| `Dashboard.jsx` | 대시보드 | 메인 화면, 포지션/PnL 표시 |
| `Trading.jsx` | 트레이딩 | 실시간 차트, 주문 |
| `TradingHistory.jsx` | 거래 내역 | 과거 거래 목록 |
| `BotManagement.jsx` | ⭐ 봇 관리 | 다중 봇 관리 UI |
| `BacktestingPage.jsx` | 백테스팅 | 전략 테스트 |
| `Settings.jsx` | 설정 | API 키, 알림 설정 |
| `Login.jsx` | 로그인/회원가입 | 인증 |

#### 컴포넌트 (`frontend/src/components/`)

| 디렉토리 | 주요 컴포넌트 |
|----------|--------------|
| `bot/` | BotCard, AddBotCard, AllocationBar |
| `grid/` | GridVisualizer, CreateGridBotModal, GridBotCard |
| `chart/` | TradingChart |
| `layout/` | MainLayout, Sidebar |
| `strategy/` | BacktestRunner |

#### API 클라이언트 (`frontend/src/api/`)

| 파일 | 역할 |
|------|------|
| `client.js` | ⭐ HTTP 클라이언트 (axios) |
| `botInstances.js` | 봇 인스턴스 API |
| `gridBot.js` | 그리드 봇 API |
| `auth.js` | 인증 API |

---

## 🔄 데이터 흐름

### 1. 봇 실행 흐름

```
사용자 → "봇 시작" 클릭
      │
      ▼
┌─────────────────────────────────────────────────────────────────┐
│  Frontend: BotCard.jsx                                          │
│  botInstances.start(botId) 호출                                 │
└─────────────────────────────────────────────────────────────────┘
      │
      ▼ POST /api/v1/bot-instances/{id}/start
┌─────────────────────────────────────────────────────────────────┐
│  Backend: api/bot_instances.py                                  │
│  start_bot_instance() 엔드포인트                                │
└─────────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────────┐
│  Backend: services/bot_runner.py                                │
│  BotRunner.start_instance(bot_instance_id, user_id)             │
│                                                                 │
│  _run_instance_loop():                                          │
│    1. BotInstance 테이블에서 설정 로드                           │
│    2. 전략 로드 (strategy_loader.py)                            │
│    3. 시장 데이터 수신 (candle_cache.py)                         │
│    4. 시그널 생성 (strategy.analyze_and_decide())               │
│    5. 익절/손절 체크                                            │
│    6. 주문 실행 (bitget_rest.py)                                 │
│    7. 거래 기록 저장 (trades 테이블)                             │
│    8. WebSocket 브로드캐스트                                     │
│    9. 루프 반복 (5초마다)                                       │
└─────────────────────────────────────────────────────────────────┘
```

### 2. AI 분석 흐름

```
봇 실행 중
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  strategy.analyze_and_decide() 호출                             │
└─────────────────────────────────────────────────────────────────┘
    │
    ├── MarketRegimeAgent.analyze_market_realtime()
    │       │
    │       ├── 규칙 기반 분석 (ADX, ATR, 볼린저 등)
    │       │
    │       └── DeepSeek AI 분석 (옵션)
    │               │
    │               ▼
    │           deepseek_service._make_request()
    │               │
    │               ▼
    │           "시장 환경: TRENDING, 신뢰도: 0.85"
    │
    ├── SignalValidatorAgent.validate()
    │       │ 시그널 유효성 검증
    │
    ├── RiskMonitorAgent.assess()
    │       │ 리스크 레벨 평가
    │
    └── 최종 결정
            │
            ▼
        { action: "buy", confidence: 0.75, ... }
```

### 3. 사용자 데이터 격리 흐름

```
1번 회원 API 요청
    │
    ▼ Authorization: Bearer <JWT>
┌─────────────────────────────────────────────────────────────────┐
│  JWT 검증 → user_id = 1 추출                                    │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│  DB 쿼리 시 자동 필터링:                                        │
│                                                                 │
│  SELECT * FROM trades WHERE user_id = 1                         │
│  SELECT * FROM bot_instances WHERE user_id = 1                  │
│  SELECT * FROM strategies WHERE user_id = 1 OR user_id IS NULL  │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
1번 회원 데이터만 반환 (2번 회원 데이터 접근 불가)
```

---

## 👥 다중 사용자 시스템

### 데이터 격리 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│                      User 1 (user_id = 1)                       │
├─────────────────────────────────────────────────────────────────┤
│  bot_instances   │ user_id = 1인 봇만 접근/조회 가능            │
│  trades          │ user_id = 1인 거래만 조회                    │
│  api_keys        │ user_id = 1인 키만 (암호화 저장)             │
│  strategies      │ user_id = 1 또는 NULL(공용)만               │
│  backtest_results│ user_id = 1인 결과만                        │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      User 2 (user_id = 2)                       │
├─────────────────────────────────────────────────────────────────┤
│  bot_instances   │ user_id = 2인 봇만 접근/조회 가능            │
│  trades          │ user_id = 2인 거래만 조회                    │
│  ... (동일)                                                     │
└─────────────────────────────────────────────────────────────────┘
```

### 주요 테이블 관계

```sql
users (id)
    ├── api_keys (user_id FK)
    ├── strategies (user_id FK, nullable)
    ├── bot_instances (user_id FK)
    │       └── grid_bot_configs (bot_instance_id FK)
    │               └── grid_orders (grid_config_id FK)
    ├── trades (user_id FK)
    ├── backtest_results (user_id FK)
    └── bot_status (user_id PK)
```

---

## 🤖 AI 에이전트 시스템

### 4개 AI 에이전트 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                    ETH Autonomous Strategy                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌──────────────────────┐    ┌──────────────────────┐          │
│   │  📊 MarketRegimeAgent │    │  ✅ SignalValidator  │          │
│   │                      │    │     Agent            │          │
│   │  - 시장 환경 분석     │    │  - 시그널 유효성     │          │
│   │  - 추세/횡보/변동성   │    │  - 중복 진입 방지    │          │
│   │  - DeepSeek AI 연동  │    │  - 신뢰도 검증       │          │
│   └──────────────────────┘    └──────────────────────┘          │
│                                                                 │
│   ┌──────────────────────┐    ┌──────────────────────┐          │
│   │  ⚠️ RiskMonitorAgent │    │  📈 PortfolioOptimizer│          │
│   │                      │    │     Agent            │          │
│   │  - 실시간 리스크 감시│    │  - 포지션 사이즈     │          │
│   │  - 청산가 경고       │    │  - 5~40% 마진 범위   │          │
│   │  - 일일 손실 한도    │    │  - 동적 레버리지     │          │
│   └──────────────────────┘    └──────────────────────┘          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 에이전트 호출 순서

1. **MarketRegimeAgent** → 시장 환경 분석 (trending, ranging, volatile, low_volume)
2. **SignalValidatorAgent** → 시그널 유효성 검증
3. **RiskMonitorAgent** → 리스크 레벨 평가
4. **PortfolioOptimizerAgent** → 최적 포지션 사이즈 결정

---

## 📖 작업별 참고 문서

### 🔧 작업 시작 전 필독

| 작업 유형 | 필독 문서 | 우선순위 |
|----------|----------|---------|
| **모든 작업** | `CLAUDE.md` | ⭐⭐⭐ |
| **모든 작업** | 이 문서 (`CODEBASE_OVERVIEW.md`) | ⭐⭐⭐ |

### 📊 백엔드 작업

| 작업 | 참고 문서 |
|------|----------|
| API 엔드포인트 추가 | `docs/BACKEND_ARCHITECTURE.md` |
| DB 스키마 수정 | `docs/MULTI_BOT_02_DATABASE.md` |
| 봇 로직 수정 | `CLAUDE.md` → "bot_runner.py 수정 규칙" |
| 전략 추가/수정 | `backend/STRATEGY_GUIDE.md` |
| AI 에이전트 수정 | `backend/src/agents/README.md` |
| 다중 봇 기능 | `docs/MULTI_BOT_03_IMPLEMENTATION.md` |

### 🎨 프론트엔드 작업

| 작업 | 참고 문서 |
|------|----------|
| 페이지/컴포넌트 추가 | `docs/FRONTEND_ARCHITECTURE.md` |
| 차트 관련 | `docs/FRONTEND_CHART_IMPLEMENTATION.md` |
| 봇 UI 수정 | `docs/MULTI_BOT_01_OVERVIEW.md` (Phase 4) |

### 🚀 배포 작업

| 작업 | 참고 문서 |
|------|----------|
| 서버 배포 | `docs/DEPLOYMENT_GUIDE.md` |
| 배포 체크리스트 | `CLAUDE.md` → "배포 프로세스" |

### 🔬 ML/AI 작업

| 작업 | 참고 문서 |
|------|----------|
| ML 모델 통합 | `docs/ML_MODEL_INTEGRATION_PLAN.md` |

---

## 💻 자주 사용하는 명령어

### 로컬 개발

```bash
# 백엔드 실행
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn src.main:app --reload --port 8000

# 프론트엔드 실행
cd frontend
npm install
npm run dev  # port 3000

# 관리자 프론트엔드
cd admin-frontend
npm install
npm run dev  # port 4000

# Docker로 전체 실행
docker compose up -d
```

### 데이터베이스

```bash
# 마이그레이션 생성
cd backend
alembic revision --autogenerate -m "설명"

# 마이그레이션 적용
alembic upgrade head

# 마이그레이션 롤백
alembic downgrade -1
```

### 서버 배포 (상세: CLAUDE.md 참조)

```bash
# 빠른 동기화 후 재시작
rsync -avz ./ root@158.247.245.197:/root/auto-dashboard/ \
  --exclude node_modules --exclude .git --exclude __pycache__
  
sshpass -p 'Vc8,xn7j_fjdnNGy' ssh root@158.247.245.197 \
  "cd /root/auto-dashboard && docker compose build --no-cache backend && docker compose up -d backend"
```

### 로그 확인

```bash
# 백엔드 로그
docker logs trading-backend --tail 100 -f

# 프론트엔드 로그
docker logs trading-frontend --tail 100 -f

# 에러만 확인
docker logs trading-backend 2>&1 | grep -E "ERROR|Exception|Traceback"
```

---

## ⚠️ 주의사항

### 🔴 절대 금지 사항

| 금지 사항 | 이유 |
|----------|------|
| `docker cp`만으로 배포 완료 선언 | 컨테이너 재시작 시 사라짐 |
| 40% 마진 한도 변경 | 리스크 관리 핵심 |
| `current_position` 구조 변경 | 여러 파일에서 사용 |
| 프론트 빌드 시 `--no-cache` 미사용 | API URL 캐시 문제 |

### 🟡 주의 사항

| 주의 사항 | 해결 방법 |
|----------|----------|
| `bot_runner.py`는 2700줄 | 두 개의 루프(instance/legacy) 모두 수정 확인 |
| 사용자 데이터 격리 | 모든 쿼리에 `user_id` 조건 확인 |
| AI API 비용 | DeepSeek 호출 횟수 최적화 필요 |

### 🟢 권장 사항

| 권장 사항 | 설명 |
|----------|------|
| 문법 검증 후 배포 | `python -m py_compile 파일.py` |
| 작은 단위로 커밋 | 롤백 용이 |
| 로그 확인 필수 | 배포 후 `docker logs` 확인 |

---

## 📞 서버 정보

```
Production Server: 158.247.245.197
SSH: root / Vc8,xn7j_fjdnNGy
Project Path: /root/auto-dashboard

도메인:
- 사용자: https://ai-deepsignal.com
- 관리자: https://admin.ai-deepsignal.com
- API: https://api.ai-deepsignal.com
```

---

## 📊 프로젝트 현황

| 항목 | 상태 |
|------|------|
| 백엔드 완성도 | 95% |
| 프론트엔드 완성도 | 90% |
| 다중 봇 시스템 | ✅ 완료 |
| AI 에이전트 | ✅ 완료 |
| ML 모델 통합 | ⬜ 계획 중 |
| 테스트 커버리지 | ~30% |

---

**작성자**: AI Assistant  
**최종 업데이트**: 2025-12-20  
**버전**: 1.0

# 🚀 AI + LightGBM 최적 조합 계획서

# DeepSeek AI + LightGBM 하이브리드 시스템

---

## 📌 문서 정보

| 항목 | 내용 |
|------|------|
| 작성일 | 2025-12-20 |
| 버전 | 4.0 (AI 하이브리드 버전) |
| 프로젝트 | auto-dashboard |
| 예상 기간 | **3~4주** |
| 핵심 철학 | **AI의 추론력 + ML의 정밀도 = 최적의 조합** |

---

## 🎯 핵심 철학: AI와 ML의 역할 분담

### 각 기술의 강점

| 기술 | 강점 | 약점 | 최적 활용 |
|------|------|------|----------|
| **DeepSeek AI (LLM)** | 맥락 이해, 추론, 예외 상황 판단 | 느림, 비용, 수치 정밀도 낮음 | 큰 그림, 시장 해석, 위험 경고 |
| **LightGBM (ML)** | 빠름, 정밀, 일관성, 피처 기반 | 맥락 이해 못함, 학습 데이터 의존 | 수치 예측, 필터링, 최적화 |
| **규칙 기반** | 신뢰성, 해석 가능, 빠름 | 유연성 부족, 시장 변화 적응 어려움 | 기본 신호, 안전장치 |

### 🌟 최적 조합 원칙

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        역할 분담 원칙                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   🧠 DeepSeek AI (감독자 역할)                                          │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                     │
│   "지금 시장이 어떤 상황이야? 특별한 이벤트 있어?"                        │
│   → 시장 환경 해석 (Trending, Ranging, Volatile)                        │
│   → 예외 상황 감지 (뉴스, 급등락, 이상 패턴)                              │
│   → 최종 거부권 (AI가 "위험하다"고 판단하면 진입 차단)                    │
│                                                                         │
│   📊 LightGBM (실무자 역할)                                              │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                     │
│   "구체적으로 언제, 얼마나, 어느 가격에?"                                 │
│   → 진입 타이밍: "지금이 좋은 진입 시점인가?" (Yes/No)                   │
│   → 방향 확인: 규칙 기반 신호를 확인/거부                                 │
│   → 수치 최적화: 손절폭, 포지션 사이즈, 레버리지                          │
│                                                                         │
│   📐 규칙 기반 (기초 역할)                                               │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━                     │
│   "기본 신호는 뭐야?"                                                    │
│   → EMA 크로스, RSI 과매수/과매도, ATR 기반 SL/TP                        │
│   → 40% 마진 한도 강제 (절대 변경 금지)                                   │
│   → 일일 손실 한도 체크                                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 의사결정 흐름 (AI + ML 하이브리드)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    의사결정 파이프라인                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   [Stage 1] 규칙 기반 - 기본 신호 생성 (1ms)                             │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                                                         │
│   ┌────────────────────────────────────────────────────────────────┐    │
│   │  📐 RuleBasedAnalyzer                                          │    │
│   │  - EMA 크로스 체크                                              │    │
│   │  - RSI 과매수/과매도                                            │    │
│   │  - 볼린저 밴드 위치                                              │    │
│   │  → 출력: base_signal = "long" / "short" / "hold"               │    │
│   └────────────────────────────────────────────────────────────────┘    │
│                              │                                          │
│                              ▼                                          │
│   [Stage 2] LightGBM - 신호 검증 & 최적화 (10ms)                        │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  5개 LightGBM 모델 (병렬 추론)                                   │   │
│   │                                                                 │   │
│   │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌──────────┐   │   │
│   │  │ 방향    │ │ 변동성  │ │ 타이밍  │ │ 최적SL  │ │ 포지션   │   │   │
│   │  │ 확인    │ │ 예측    │ │ 검증    │ │ 예측    │ │ 사이즈   │   │   │
│   │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └──────────┘   │   │
│   │                                                                 │   │
│   │  → 규칙 기반 신호와 일치하면 신뢰도 ↑                            │   │
│   │  → 불일치하면 신뢰도 ↓ 또는 거부                                 │   │
│   │  → 타이밍 "No"면 진입 보류                                      │   │
│   │  → 변동성 "Extreme"이면 진입 거부                                │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              ▼                                          │
│   [Stage 3] DeepSeek AI - 최종 확인 (200~500ms)                         │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  🧠 MarketRegimeAgent + SignalValidatorAgent                    │   │
│   │                                                                 │   │
│   │  호출 조건 (비용 절약):                                          │   │
│   │  - Stage 2까지 신뢰도 > 0.6 인 경우에만 AI 호출                  │   │
│   │  - 또는 5분마다 1회 (시장 환경 업데이트)                         │   │
│   │                                                                 │   │
│   │  AI 역할:                                                       │   │
│   │  - "현재 시장 환경에서 이 신호가 합리적인가?"                    │   │
│   │  - "특이한 패턴이나 위험 요소가 있는가?"                         │   │
│   │  - "규칙+ML이 놓친 맥락이 있는가?"                               │   │
│   │                                                                 │   │
│   │  AI 판단:                                                       │   │
│   │  - "APPROVE": 진입 승인                                         │   │
│   │  - "REJECT": 진입 거부 + 이유                                   │   │
│   │  - "CAUTION": 사이즈 50% 축소 후 진입                           │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              ▼                                          │
│   [Stage 4] 최종 실행                                                   │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                                                         │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  FinalDecision = {                                              │   │
│   │    action: "long",                                              │   │
│   │    confidence: 0.78,        # 종합 신뢰도                       │   │
│   │    position_size: 25%,      # LightGBM 최적화                   │   │
│   │    leverage: 8x,            # 변동성 기반 조정                   │   │
│   │    stop_loss: 1.2%,         # LightGBM 최적화                   │   │
│   │    take_profit: 2.4%,       # SL × 2                           │   │
│   │    reasoning: "규칙:Long + LGB:확인 + AI:승인",                 │   │
│   │  }                                                              │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 각 단계별 필터링 효과

```
100개 신호 발생
        │
        ▼
[Stage 1] 규칙 기반 신호
        │ (100개)
        ▼
[Stage 2] LightGBM 필터링
        │ - 타이밍 "No" → 제거 (-30개)
        │ - 방향 불일치 → 제거 (-15개)
        │ - 변동성 Extreme → 제거 (-5개)
        │ (50개 남음)
        ▼
[Stage 3] DeepSeek AI 확인
        │ - "REJECT" → 제거 (-10개)
        │ - "CAUTION" → 사이즈 축소 (10개)
        │ (40개 정상 진입, 10개 축소 진입)
        ▼
최종 진입: 50개 (사이즈 조정 포함)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 대비 효과:
- 거짓 신호 50개 제거 (50% 감소)
- 남은 50개 중 더 높은 품질
- AI 비용 절약 (50개만 AI 호출, 기존 100개)
```

---

## 🧠 AI와 LightGBM의 협력 시나리오

### 시나리오 1: 일반적인 상승장

```
📐 규칙 기반: "Long 신호 (EMA 크로스, RSI 50)"
📊 LightGBM:
   - 방향: Long (75% 확신) ✅
   - 타이밍: Good ✅
   - 변동성: Normal
   - 최적 SL: 1.5%
   - 최적 Size: 30%

🧠 DeepSeek AI: (호출됨)
   "현재 시장은 명확한 상승 추세입니다. 
    규칙과 ML 신호가 일치하며, 특별한 위험 요소가 없습니다.
    APPROVE - 정상 진입 권장"

✅ 최종: Long 진입, 30% 사이즈, SL 1.5%
```

### 시나리오 2: 거짓 신호 필터링

```
📐 규칙 기반: "Long 신호 (EMA 크로스, RSI 52)"
📊 LightGBM:
   - 방향: Short (62% 확신) ❌ 불일치
   - 타이밍: Bad ❌
   - 변동성: High

🧠 DeepSeek AI: (호출 안됨 - 이미 LightGBM에서 거부)

❌ 최종: Hold - LightGBM이 거부
   이유: "방향 불일치 + 진입 타이밍 부적절"
```

### 시나리오 3: AI가 거부하는 경우

```
📐 규칙 기반: "Long 신호"
📊 LightGBM:
   - 방향: Long (68%) ✅
   - 타이밍: Good ✅
   - 변동성: High (하지만 Extreme 아님)

🧠 DeepSeek AI: (호출됨)
   "규칙과 ML은 Long을 추천하지만,
    현재 비트코인이 주요 저항선($100K)에 근접해 있고,
    과거 이 구간에서 여러 번 거부당한 이력이 있습니다.
    CAUTION - 포지션 사이즈 50% 축소 권장"

⚠️ 최종: Long 진입, 사이즈 15% (30% × 0.5), SL 1.8%
```

### 시나리오 4: 극단적 변동성

```
📐 규칙 기반: "Long 신호 (급등 후 RSI 30)"
📊 LightGBM:
   - 변동성: Extreme ❌
   - (나머지 분석 불필요 - 즉시 Hold)

🧠 DeepSeek AI: (호출 안됨)

❌ 최종: Hold - 극단적 변동성으로 진입 회피
   이유: "ATR이 평균 대비 300% - 신규 진입 위험"
```

---

## 🔧 기존 에이전트와의 통합

### 현재 4개 AI 에이전트 + LightGBM

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    확장된 에이전트 시스템                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   기존 에이전트 (유지)                                                   │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                                                         │
│   ┌──────────────────────┐    ┌──────────────────────┐                  │
│   │  🧠 MarketRegimeAgent │    │  ✅ SignalValidator  │                  │
│   │  (DeepSeek AI)       │    │     Agent            │                  │
│   │                      │    │                      │                  │
│   │  [변경] LightGBM     │    │  [변경] LightGBM     │                  │
│   │  변동성 예측 통합     │    │  타이밍 예측 통합     │                  │
│   └──────────────────────┘    └──────────────────────┘                  │
│                                                                         │
│   ┌──────────────────────┐    ┌──────────────────────┐                  │
│   │  ⚠️ RiskMonitorAgent │    │  📈 PortfolioOptimizer│                  │
│   │                      │    │     Agent            │                  │
│   │  [변경] LightGBM     │    │  [변경] LightGBM     │                  │
│   │  손절폭 예측 통합     │    │  포지션 사이즈 통합   │                  │
│   └──────────────────────┘    └──────────────────────┘                  │
│                                                                         │
│   신규 추가                                                              │
│   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━       │
│                                                                         │
│   ┌──────────────────────────────────────────────────────────────┐      │
│   │  📊 MLPredictorAgent (NEW)                                   │      │
│   │                                                              │      │
│   │  - 5개 LightGBM 모델 통합 관리                                │      │
│   │  - 피처 파이프라인 실행                                       │      │
│   │  - 예측 결과 캐싱                                            │      │
│   │  - 기존 에이전트에 결과 제공                                  │      │
│   └──────────────────────────────────────────────────────────────┘      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 에이전트별 LightGBM 활용

| 에이전트 | 기존 역할 | + LightGBM 활용 |
|---------|---------|-----------------|
| **MarketRegimeAgent** | AI로 시장 환경 분석 | + 변동성 예측 (Model 2) 검증 |
| **SignalValidatorAgent** | AI로 신호 검증 | + 타이밍 예측 (Model 3) 사전 필터링 |
| **RiskMonitorAgent** | 리스크 감시 | + 최적 손절폭 (Model 4) 권장 |
| **PortfolioOptimizerAgent** | 포지션 최적화 | + 최적 사이즈 (Model 5) 권장 |
| **MLPredictorAgent** (NEW) | - | 5개 모델 통합 관리 |

---

## 📁 파일 구조

```
backend/src/
├── ml/                                    # [NEW] LightGBM 모듈
│   ├── __init__.py
│   ├── config.py                          # 설정 (피처, 하이퍼파라미터)
│   │
│   ├── features/
│   │   ├── __init__.py
│   │   ├── technical_features.py          # 기술적 피처 (50개)
│   │   ├── structure_features.py          # 시장 구조 피처 (10개)
│   │   ├── mtf_features.py                # 멀티타임프레임 피처 (10개)
│   │   └── feature_pipeline.py            # 통합 파이프라인
│   │
│   ├── models/
│   │   ├── __init__.py
│   │   ├── direction_model.py             # Model 1: 방향 확인
│   │   ├── volatility_model.py            # Model 2: 변동성 예측
│   │   ├── timing_model.py                # Model 3: 진입 타이밍
│   │   ├── stoploss_model.py              # Model 4: 최적 손절폭
│   │   ├── position_size_model.py         # Model 5: 최적 사이즈
│   │   └── ensemble_predictor.py          # 5개 모델 통합
│   │
│   └── training/
│       ├── __init__.py
│       ├── data_collector.py              # 학습 데이터 수집
│       ├── labeler.py                     # 자동 레이블링
│       └── train_all_models.py            # 전체 모델 학습
│
├── agents/
│   ├── ml_predictor/                      # [NEW] ML 예측 에이전트
│   │   ├── __init__.py
│   │   ├── agent.py                       # MLPredictorAgent
│   │   └── models.py                      # 출력 데이터 모델
│   │
│   ├── market_regime/
│   │   └── agent.py                       # [MODIFY] LightGBM 통합
│   │
│   └── signal_validator/
│       └── agent.py                       # [MODIFY] LightGBM 통합
│
├── services/
│   └── strategy_loader.py                 # [MODIFY] ML 통합
│
└── strategies/
    └── eth_ai_autonomous_40pct_strategy.py # [MODIFY] AI+ML 파이프라인
```

---

## 🔄 코드 통합 예시

### 전략 코드 수정 (`eth_ai_autonomous_40pct_strategy.py`)

```python
class EthAIAutonomous40PctStrategy:
    
    def __init__(self, ...):
        # 기존 초기화 (유지)
        self.market_regime_agent = MarketRegimeAgent(...)
        self.signal_validator = SignalValidatorAgent(...)
        self.risk_monitor = RiskMonitorAgent(...)
        self.portfolio_optimizer = PortfolioOptimizerAgent(...)
        
        # [NEW] LightGBM 모듈 추가
        self.ml_predictor = get_ml_predictor_agent()
        
    async def analyze_and_decide(
        self,
        current_price: float,
        candles: list,
        current_position: Optional[PositionInfo] = None,
    ) -> AutonomousDecision:
        """
        AI + LightGBM 하이브리드 의사결정
        """
        
        # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        # Stage 1: 규칙 기반 신호 (기존 코드 유지)
        # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        base_signal = self._analyze_technical_indicators(candles, current_price)
        
        if base_signal.action == "hold":
            return self._create_hold_decision("규칙 기반: 신호 없음")
        
        # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        # Stage 2: LightGBM 검증 & 최적화 [NEW]
        # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        ml_result = await self.ml_predictor.predict(
            symbol=self.symbol,
            candles_5m=candles,
            candles_1h=self._get_hourly_candles(),
        )
        
        # 2-1. 극단적 변동성 체크
        if ml_result.volatility.level == "extreme":
            return self._create_hold_decision(
                f"LightGBM: 극단적 변동성 ({ml_result.volatility.atr_ratio:.1f}x)"
            )
        
        # 2-2. 타이밍 체크
        if ml_result.timing.is_good_entry == False and ml_result.timing.confidence > 0.6:
            return self._create_hold_decision(
                f"LightGBM: 진입 타이밍 부적절 ({ml_result.timing.confidence:.0%})"
            )
        
        # 2-3. 방향 불일치 체크
        if ml_result.direction.direction != base_signal.action:
            if ml_result.direction.confidence > 0.65:
                return self._create_hold_decision(
                    f"LightGBM 방향 불일치: 규칙={base_signal.action}, ML={ml_result.direction.direction}"
                )
            else:
                # 약한 불일치 - 신뢰도 하락으로 처리
                base_signal.confidence *= 0.7
        
        # 2-4. 신뢰도 계산
        combined_confidence = self._calculate_combined_confidence(
            rule_confidence=base_signal.confidence,
            ml_result=ml_result,
        )
        
        if combined_confidence < 0.55:
            return self._create_hold_decision(
                f"통합 신뢰도 부족: {combined_confidence:.0%}"
            )
        
        # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        # Stage 3: DeepSeek AI 최종 확인 (기존 로직 + 비용 최적화)
        # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        
        # AI 호출 조건: 신뢰도 > 0.6 이상인 경우만 (비용 절약)
        ai_approval = True
        ai_caution = False
        ai_reasoning = ""
        
        if combined_confidence > 0.6:
            ai_result = await self.market_regime_agent.analyze_market_realtime({
                "symbol": self.symbol,
                "candles": candles[-60:],  # 최근 60개
            })
            
            validation = await self.signal_validator.validate({
                "signal": base_signal.action,
                "ml_result": ml_result.to_dict(),
                "regime": ai_result.regime_type,
            })
            
            if validation.status == "REJECT":
                return self._create_hold_decision(
                    f"AI 거부: {validation.reason}"
                )
            elif validation.status == "CAUTION":
                ai_caution = True
                ai_reasoning = validation.reason
        
        # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        # Stage 4: 최종 결정 조합
        # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        
        # 변동성 기반 조정
        vol_adj = VOLATILITY_ADJUSTMENTS[ml_result.volatility.level]
        
        # 최종 포지션 사이즈
        final_size = ml_result.position_size.optimal_size * vol_adj["size_mult"]
        if ai_caution:
            final_size *= 0.5  # AI 경고 시 50% 축소
        final_size = min(max(final_size, 5.0), 40.0)  # 5~40% 범위
        
        # 최종 손절폭
        final_sl = ml_result.stoploss.optimal_sl * vol_adj["sl_mult"]
        final_sl = min(max(final_sl, 0.5), 3.0)  # 0.5~3% 범위
        
        # 최종 레버리지
        final_leverage = int(10 * vol_adj["leverage_mult"])
        final_leverage = min(max(final_leverage, 3), 15)  # 3~15x 범위
        
        return AutonomousDecision(
            decision=TradingDecision.ENTER_LONG if base_signal.action == "long" else TradingDecision.ENTER_SHORT,
            confidence=combined_confidence,
            position_size_percent=final_size,
            target_leverage=final_leverage,
            stop_loss_percent=final_sl,
            take_profit_percent=final_sl * 2.0,
            reasoning=(
                f"규칙:{base_signal.action} + "
                f"LGB:{ml_result.direction.direction}({ml_result.direction.confidence:.0%}) + "
                f"AI:{'경고' if ai_caution else '승인'}"
            ),
            ai_enhanced=True,
        )
```

---

## 📊 예상 성과

### 단계별 효과

| 단계 | 필터링 효과 | 비용 |
|------|------------|------|
| Stage 1 (규칙) | 기본 신호 생성 | 무료 |
| Stage 2 (LightGBM) | 거짓 신호 50% 필터링 | CPU 10ms |
| Stage 3 (DeepSeek AI) | 추가 20% 필터링 | ~$0.0002/호출 |

### 종합 성과

| 지표 | 현재 | AI+ML 적용 후 | 개선 |
|------|------|--------------|------|
| 승률 | ~45% | ~55-60% | **+10-15%p** |
| 거짓 신호 | 기준 | -50~70% | **대폭 감소** |
| 평균 손실 | -1.5% | -1.0% | **33% 감소** |
| AI 비용 | 모든 신호 호출 | 50% 호출 | **50% 절약** |
| 위험보상비 | 1.8:1 | 2.5:1 | **개선** |

---

## ✅ 구현 체크리스트 (3~4주)

### Week 1: 피처 파이프라인 (15h)

- [ ] ML 모듈 구조 생성
- [ ] 기술적 피처 50개 구현
- [ ] 구조/MTF 피처 20개 구현
- [ ] FeaturePipeline 클래스

### Week 2: 모델 학습 (19h)

- [ ] 학습 데이터 수집 (1년)
- [ ] 자동 레이블링 시스템
- [ ] 5개 LightGBM 모델 학습
- [ ] EnsemblePredictor 구현

### Week 3: 에이전트 통합 (19h)

- [ ] MLPredictorAgent 구현
- [ ] 기존 에이전트에 ML 통합
- [ ] 전략 코드 수정
- [ ] 백테스트 검증

### Week 4: 검증 & 배포 (10h+)

- [ ] A/B 테스트
- [ ] 페이퍼 트레이딩
- [ ] 프로덕션 배포
- [ ] 모니터링 설정

---

## 🎯 핵심 요약

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    AI + LightGBM 최적 조합                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   📐 규칙 기반: "기본 신호 생성" (빠름, 신뢰성)                          │
│                       │                                                 │
│                       ▼                                                 │
│   📊 LightGBM: "신호 검증 & 수치 최적화" (빠름, 정밀)                   │
│        - 거짓 신호 50% 필터링                                           │
│        - 최적 SL/사이즈 계산                                            │
│                       │                                                 │
│                       ▼                                                 │
│   🧠 DeepSeek AI: "최종 확인 & 예외 처리" (느림, 추론력)                │
│        - 맥락 기반 거부권                                               │
│        - 비용 최적화 (50% 호출만)                                       │
│                       │                                                 │
│                       ▼                                                 │
│   ✅ 최종 결정: 고품질 신호만 진입                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

**작성자**: AI Assistant  
**최종 업데이트**: 2025-12-20  
**버전**: 4.0 (AI 하이브리드 최적화)

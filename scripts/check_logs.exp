#!/usr/bin/expect -f
# Check backend logs

set timeout 60
set password "Vc8,xn7j_fjdnNGy"

spawn ssh root@158.247.245.197 "cd /root/auto-dashboard && docker compose --env-file .env.production logs backend --tail=100"

expect {
    "*password:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
